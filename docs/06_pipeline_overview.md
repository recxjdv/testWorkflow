The workflow runs a single process:

1. **runRecx** — executes `recx.sh` inside an Ubuntu 22.04 container, which reports the current date and mount information, then copies the result to the output directory.
